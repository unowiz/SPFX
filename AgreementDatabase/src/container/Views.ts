export default class Views {
    public Title: string;
    public Id: string;
    constructor(
        Title: string,
        Id: string
    ) {
        this.Title = Title;
        this.Id = Id;
    }
}